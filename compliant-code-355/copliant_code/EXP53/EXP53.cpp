#include <iostream>
#include <string>

using namespace std;

/**
 * @brief A simple program that prints the value of an initialized string
 */
int main() {
    string name = "Marco";
    cout << "The name is: " << name << endl; //Since name is initialize, we are compliant when reading the data
    return 0;
}

